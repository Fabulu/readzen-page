{
  "name": "zenlinkpage",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/",
    "build": "node build/build-dict-shards.js",
    "build:seo": "node build/generate-seo-pages.js",
    "build:seo:force": "node build/generate-seo-pages.js --force",
    "build:search": "node --max-old-space-size=4096 --expose-gc build/build-bigram-index.js && node build/build-english-jsonl.js && npm run build:seo",
    "dist": "node build/make-dist.js",
    "deploy": "npm run build && npm run build:search && node build/make-dist.js && npx wrangler pages deploy dist --project-name=readzen --branch=main"
  },
  "dependencies": {}
}
