{
  "name": "zenlinkpage",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/",
    "build": "node build/build-dict-shards.js",
    "build:search": "node build/build-pagefind-index.js",
    "deploy": "npm run build"
  },
  "dependencies": {
    "pagefind": "^1.5.2"
  }
}
